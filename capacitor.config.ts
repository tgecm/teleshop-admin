import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecommercemyanmar.shop',
  appName: 'E-commerce',
  webDir: 'dist',
  server: {
    androidScheme: 'http',
  },
};

export default config;
